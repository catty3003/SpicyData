<%= form_for(@admin_matrix) do |f| %>
  <% if @admin_matrix.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_matrix.errors.count, "error") %> prohibited this admin_matrix from being saved:</h2>

      <ul>
      <% @admin_matrix.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
