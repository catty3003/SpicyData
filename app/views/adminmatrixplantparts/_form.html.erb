<%= form_for(@adminmatrixplantpart) do |f| %>
  <% if @adminmatrixplantpart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@adminmatrixplantpart.errors.count, "error") %> prohibited this adminmatrixplantpart from being saved:</h2>

      <ul>
      <% @adminmatrixplantpart.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :plant_part %><br>
    <%= f.text_field :plant_part %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
