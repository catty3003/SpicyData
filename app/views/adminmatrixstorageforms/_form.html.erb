<%= form_for(@adminmatrixstorageform) do |f| %>
  <% if @adminmatrixstorageform.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@adminmatrixstorageform.errors.count, "error") %> prohibited this adminmatrixstorageform from being saved:</h2>

      <ul>
      <% @adminmatrixstorageform.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :storage_form %><br>
    <%= f.text_field :storage_form %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
