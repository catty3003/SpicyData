<%= form_for(@admintreatmentprocesstype) do |f| %>
  <% if @admintreatmentprocesstype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admintreatmentprocesstype.errors.count, "error") %> prohibited this admintreatmentprocesstype from being saved:</h2>

      <ul>
      <% @admintreatmentprocesstype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :process_type %><br>
    <%= f.text_field :process_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
