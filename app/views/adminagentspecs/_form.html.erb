<%= form_for(@adminagentspec) do |f| %>
  <% if @adminagentspec.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@adminagentspec.errors.count, "error") %> prohibited this adminagentspec from being saved:</h2>

      <ul>
      <% @adminagentspec.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :a_species %><br>
    <%= f.text_field :a_species %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
