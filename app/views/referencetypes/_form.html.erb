<%= form_for(@referencetype) do |f| %>
  <% if @referencetype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@referencetype.errors.count, "error") %> prohibited this referencetype from being saved:</h2>

      <ul>
      <% @referencetype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type_of_literatur %><br>
    <%= f.text_field :type_of_literatur %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
