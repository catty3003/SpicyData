<%= form_for(@adminmatrixproductform) do |f| %>
  <% if @adminmatrixproductform.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@adminmatrixproductform.errors.count, "error") %> prohibited this adminmatrixproductform from being saved:</h2>

      <ul>
      <% @adminmatrixproductform.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_form %><br>
    <%= f.text_field :product_form %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
